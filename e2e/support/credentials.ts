// Single source of truth for the seeded test account: the global setup passes
// these to the Python seeder, and the auth spec logs in with them.

export const E2E_EMAIL = "e2e-user@example.com";
export const E2E_PASSWORD = "e2e-password-123";
